name: Kubernetes dev release

on:
  push:
    branches:
      - dev

permissions:
  contents: read

jobs:
  release:
    uses: IDUclub/urban-assistant-deploy/.github/workflows/reusable-application-release.yaml@main
    with:
      service: normgraph
      test-command: |
        python -m pip install --disable-pip-version-check uv==0.12.10
        uv python install 3.13
        uv sync --locked
        export NG_SERVICE_AUTH_SERVER_URL=http://localhost:9999
        export NG_SERVICE_AUTH_REALM=test
        export NG_SERVICE_AUTH_CLIENT_ID=test-client
        export NG_SERVICE_AUTH_CLIENT_SECRET=test-secret
        uv run pytest --verbose tests/unit
    secrets: inherit
